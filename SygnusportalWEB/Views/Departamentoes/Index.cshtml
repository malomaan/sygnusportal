@model IEnumerable<SygnusportalBD.Departamento>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.dep_nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pais.pai_nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.dep_nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pais.pai_nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.dep_codigo }) |
            @Html.ActionLink("Details", "Details", new { id=item.dep_codigo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.dep_codigo })
        </td>
    </tr>
}

</table>
